# Database - Alternative using current user (mac)
# Use this if you can't create clinic_user
DATABASE_URL=postgresql://mac@localhost:5432/clinic_db

# JWT
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# App
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
